### Test suite will be the "client" that is communicating with the database server which I am building


#### check if port is in use and close 
lsof -i :9999

sudo lsof -i :PORT_NUMBER

kill -9 <PID>


### 
